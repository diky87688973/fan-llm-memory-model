# 记忆模型里程碑计划

本文档描述记忆模型从 v1.0 到 v4.0 的分阶段目标、架构假设与交付边界。**v1.0 / v2.0** 为可交付主线；**v3.0 / v4.0** 为探索性里程碑，成功标准以「验证假设 + 可回滚」为主，不与主线采用同一套「按期功能完备」预期。

---

## 文档约定

- **产品叙事**：训练数据可采用第一人称「我」，表达 AI 助手在交互中的视角，便于对齐「为 AI 提供记忆能力」的定位。
- **能力边界**：「主观/自主意识中的我」指**语言与行为层面的第一人称叙事与一致性**，不表示模型获得哲学意义上的意识；对外沟通时需区分叙事与能力声明。
- **数据主权**：记忆内容仍来源于用户与对话，合规、同意、可删除等要求不因「服务于 AI」而弱化。
- **仓库布局**：本文件位于 `记忆模型/` 根目录；**v1.0 已实现代码与数据**（LoRA 管线、`session/`、`outputs/`、`hf_tokenizer/` 等）均在 **`记忆模型/1.0/`**；v2 起可在 `记忆模型/` 下并列新增版本目录。

---

## 各版本目标与可测指标（概要）

| 版本 | 类型 | 核心目标 | 建议可测指标（示例，实施时细化） |
|------|------|----------|----------------------------------|
| v1.0 | 主线（现状） | 稳定语义检索与编排闭环 | 检索准确率/召回（人工或标注集）、端到端任务成功率、服务可用性 |
| v2.0 | 主线 | 全参模型 + AI 主视角数据，流程与 v1 对齐以便对比 | 与 v1 **同一套**检索与路由评测；延迟与资源占用；相对 v1 的指标提升或持平说明 |
| v3.0 | **探索性** | 对话中增量更新参数 + 回放巩固 | 离线可复现；小规模在线 A/B；遗忘率/稳定性；**可一键回滚**到 v2 权重 |
| v4.0 | **探索性** | 记忆入口前移，弱化「有限条提取」 | 任务指标不低于探索前基线；明确内部仍受**容量/压缩**约束的等价机制 |

指标需在每版本立项时写成可执行条目（数据集、评测脚本、通过阈值）。

---

## [记忆模型] v1.0（已实现）

**定位**：基于客观语义的记忆片段检索，服务现有「大模型分析 → 记忆模型提取 → 大模型决策」流程。

| 维度 | 内容 |
|------|------|
| **架构** | 预训练基座 + LoRA 适配器微调 |
| **记忆数据** | 对话提纯后的 raw 与 qa |
| **海马体回放** | 静息时增量训练巩固记忆；采样：**当日全量 + 历史 5%** |
| **API** | 客观语义检索相关记忆片段（例：用户是否给助手取过名 → 返回对应事实片段） |
| **核心流程** | 用户输入 → 大模型（分析需检索的记忆）→ 记忆模型（提取/检索记忆）→ 大模型（决策）→ 输出 |

**说明**：v1.0 作为后续版本的**基线**；改版时应用同一套评测对比，避免流程或接口漂移导致不可比。

---

## [记忆模型] v2.0（主线）

**定位**：重新明确记忆模型为 **AI 可用的记忆检索能力**；采用自制 GPT-4.1 结构 **全参数训练**；数据以 **AI 主视角「我」** 组织所见所闻，与「人已有记忆、AI 需外挂记忆」的产品叙事一致。

| 维度 | 内容 |
|------|------|
| **架构** | 自制 GPT-4.1，全参数训练 |
| **记忆数据** | 以 AI 为核心的所见所闻均可入训：与用户对话、提纯后的 raw/qa；提纯时带入第一人称叙事（例：用户为我取名小忆，我很喜欢；他希望我能拥有自己的记忆）。QA 例：Q：用户给我取名了吗？A：是的，他给我取名叫小忆，并希望我有自己的记忆。 |
| **海马体回放** | 静息时增量训练；采样：**当日 + 历史 5%**（与 v1 历史比例一致，便于对照；具体比例可在 v2 内微调并记录） |
| **API** | **AI 主视角的语义检索**（与 v1「客观检索」对比：视角与表述统一为第一人称助手侧） |
| **核心流程** | 与 v1 相同：用户输入 → 大模型（分析提取哪些记忆）→ 记忆模型（提取/检索记忆）→ 大模型（决策）→ 输出 |

**v2.0 范围建议（写清不做的事）**：例如 **不包含**对话中实时改参（归入 v3）；**不包含**记忆模块作为唯一系统入口（归入 v4）。避免 v2 未稳即背负后续版本预期。

**成功标准（示例）**：在固定评测集上，检索/路由相关指标 **不低于 v1**，或明确记录退化原因与修复计划；推理路径与资源预算在可接受范围内。

---

## [记忆模型] v3.0（探索性里程碑）

**定位**：在 v2 全参训练与评测稳定的前提下，探索 **即时训练 v1.0**——对话过程中对参数进行增量更新；海马体回放仍以静息巩固为主，采样调整为 **历史 5%**（若与即时训练并存，需设计**冲突消解**：新写入与回放比例、避免重复学或冲掉近期记忆）。

| 维度 | 内容 |
|------|------|
| **架构** | 自制 GPT-4.1，全参数训练（延续 v2） |
| **记忆数据** | AI 为主角，以「我」的视角采集；**数据格式可在实践中再议**，向更合理、可解析、可 mask 的格式演进 |
| **即时训练 v1.0** | 一边对话一边得到参数修改（算法与工程方案探索） |
| **海马体回放** | 静息时巩固；采样：**历史 5%** |
| **API** | AI 主观视角的语义检索（与 v2 表述一致，具体是否扩展能力以探索结论为准） |
| **核心流程** | 与 v2 相同：用户输入 → 大模型 → 记忆模型 → 大模型 → 输出 |

**探索性说明**：在线更新涉及 **稳定性、灾难性遗忘、安全与滥用、回滚**。成功标准建议包含：**离线可复现**、**小规模在线验证**、**可回滚到 v2 权重**；不强制与 v2 同一交付节奏。

**依赖**：建议在 v2 评测与管线成熟后再启动 v3，否则难以区分问题来自数据、全参训练还是在线算法。

---

## [记忆模型] v4.0（探索性里程碑）

**定位**：探索 **「全部记忆参与」**，弱化「有限条提取」带来的上限感；记忆模型在系统中地位上升。**注意**：「全部激活」在工程上仍受 **上下文长度、算力与表示容量** 约束；通常需要 **压缩、路由或隐状态** 等等价机制，只是产品形态上可能不再暴露为「提取 N 条」的 API。

| 维度 | 内容 |
|------|------|
| **架构** | 自制 GPT-4.1，全参数训练 |
| **记忆数据** | AI 为主角；**数据格式再议**，向更合理格式演进 |
| **即时训练** | 延续即时训练方向（版本以当时定义为准） |
| **海马体回放** | 静息巩固；采样：**历史比例**待版本内定义（表中「历史%」保留为占位，实施时写死数值与理由） |
| **API** | 记忆模型为 **主入口之一**，**不再以「调用提取记忆 API」为唯一交互方式**（具体接口形态由探索结论决定） |
| **核心流程** | **[用户输入 / 其他输入 / 随机感知] → 记忆模型 → 大模型（决策）→ 输出** |

**探索性说明**：需同时设计 **审计、调试、用户数据删除** 在「非显式提取列表」形态下如何实现。成功标准建议包含：任务效果 **不低于探索前基线**，且内部文档写明 **容量与压缩的等价策略**，避免对「无限记忆」的字面误解。

---

## 修订记录

| 日期 | 说明 |
|------|------|
| 2026-04-12 | 初版：整合 v1–v4 设想、目标与可测指标框架、v3/v4 探索性标注及风险提示 |
| 2026-04-12 | v1 代码与数据迁入 `记忆模型/1.0/`，里程碑文件仍置于 `记忆模型/` 根目录 |
